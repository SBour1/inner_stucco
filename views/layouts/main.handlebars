<!DOCTYPE html>
<html lang="en">

<head>
    <title>STUCCO'S PIZZERIA</title>
    <link rel="preconnect" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="/css/jass.css">
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/mathjs/10.4.0/math.js" type="text/javascript"></script>
</head>

<body>
    <div class="">
        <header class="d-flex align-items-center justify-content-around">

            <h1 class="">STUCCO'S PIZZERIA</h1>
             <h3>1234 Pizza Pl, Philadelpia, PA 19111</h3>
             <h3>215-489-8317</h3>
             <h3>Open 7 days a week! 11am - 11pm</h3>
        </header>
        <nav class="navbar p-3 p-md-4 justify-content-center">
            <a href="/" class="text-white">Home</a>
            <a href="mailto:stuccospizza@gmail.com" class="text-white">Contact Us</a>
             {{#if loggedIn}}
                    <a class="text-white" id="logout">Log Out</a>
                    <a href="/cart" class="text-white" id="cartValue">Cart</a>
                {{else}}
                    <a href="/login" class="text-white">Log In</a>
                {{/if}}
            {{!-- {{#if loggedIn}}
            <button id="logout" class="btn-no-style">Logout</button>
            {{else}}
            <!-- Otherwise, show a link to login -->
            <a href="/login">Login</a>
            {{/if}} --}}
        </nav>
    </div>

    <main class="category">
        {{{body}}}
    </main>


{{#if loggedIn}}
<script src="/js/logout.js"></script>
<script src="/js/cart.js"></script>
{{/if}} 

</body>

</html>