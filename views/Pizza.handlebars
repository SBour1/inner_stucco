<div class='menu-container'>
  <div class='card-header col-md-12 d-flex flex-wrap'>
    <h2>
      Pizza Menu
    </h2>
  </div>
  {{#each pizzas as |pizza|}}
  <div class="row">
    <div class='col-sm'>
      <p>
        {{pizza.id}}.
      {{pizza.name}}
      </p>
    </div>
 <div class='col-sm'>
      <p>
    
        {{pizza.description}}
      </p>
    </div>
     <div class='col-sm'>
      <p>
      {{pizza.price}}
      
      </p>
    </div>
   <div class="col-sm">
    🥜: {{#if dish.hasNuts}} Contains nuts {{else}} No nuts {{/if}}
     {{#if dish.hasMeat}} Contains nuts {{else}} No Meat {{/if}}
  </div>
    <button class="btn-no-style">Add to cart</button>
     </div>
  {{/each}}
