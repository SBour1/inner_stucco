<section class="card">
    <a href="/menu/{{id}}">
    <header>{{name}}</header>
    <div class="card-body">
        <img src="/images/{{filename}}" alt="{{description}}">
        <p>{{price}}</p>
        <button class="btn-no-style">Add to cart!</button>
    </div>
    </a>
</section>